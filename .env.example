PORT=7000
# Optional: newline-separated manifest URLs for a fixed /manifest.json endpoint.
# UPSTREAM_ADDONS=https://v3-cinemeta.strem.io/manifest.json
# PUBLIC_BASE_URL=https://your-addon.example.com
# Set to 1 only if this private deployment must reach LAN/local addon servers.
# ALLOW_PRIVATE_UPSTREAMS=0
